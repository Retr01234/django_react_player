import React, { Component } from "react";

export default class AddRoom extends Component {
    constructor(props) {
        super(props);
    }

    render() {
        return <p>Add a Room</p>;
    }
}